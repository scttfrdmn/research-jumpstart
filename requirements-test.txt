# Testing Dependencies for Research Jumpstart
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0           # Coverage reporting
pytest-timeout>=2.1.0       # Timeout for long-running tests
pytest-xdist>=3.3.0         # Parallel test execution
pytest-mock>=3.11.0         # Mocking utilities

# AWS mocking
moto[all]>=4.2.0            # Mock AWS services (S3, DynamoDB, Lambda, etc.)
boto3>=1.28.0               # AWS SDK (for typing)
botocore>=1.31.0            # AWS SDK core

# Notebook testing
testbook>=0.4.2             # Execute and test Jupyter notebooks
nbformat>=5.9.0             # Notebook format validation
nbconvert>=7.8.0            # Notebook conversion utilities
ipykernel>=6.25.0           # Jupyter kernel for notebook execution

# Property-based and data generation
hypothesis>=6.88.0          # Property-based testing
faker>=19.6.0               # Test data generation

# Common scientific libraries (for imports)
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# NLP libraries
nltk>=3.8.0                 # Natural language processing
wordcloud>=1.9.0            # Text visualization

# Geospatial libraries (optional - large dependencies)
# rasterio>=1.3.0           # Requires GDAL - skip in CI for now

# Deep learning frameworks (optional - very large)
# torch>=2.0.0              # PyTorch - 1-2GB download, skip in CI for now

# Optional but recommended
pytest-html>=3.2.0          # HTML test reports
pytest-json-report>=1.5.0   # JSON test reports for CI/CD
